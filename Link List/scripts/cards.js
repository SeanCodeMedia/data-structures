
import { hello } from './node.js';

function setup() {

	createCanvas(windowWidth, windowHeight);
}


function draw(){

	background(52, 152, 219);
	//square((windowWidth/2)-300, (windowHeight/2)-340, 600);

}